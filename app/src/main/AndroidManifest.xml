<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sensores">

    <uses-feature
        android:name="android.hardware.sensor.proximity"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sensores">
        <activity android:name=".MenuActivity"></activity>
        <activity android:name=".GiroscopioActivity" android:label="Giroscopio"/>
        <activity android:name=".SensoresActivity" android:label="Todos Sensores"/>
        <activity android:name=".AcelerometroActivity" android:label="Acelerometro"/>
        <activity android:name=".SensorLuzActivity" android:label="Luminosidade"/>
        <activity android:name=".MainActivity" android:label="Proximidade" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>